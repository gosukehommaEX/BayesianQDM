% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.pbayesdecisionprob1bin.R
\name{plot.pbayesdecisionprob1bin}
\alias{plot.pbayesdecisionprob1bin}
\title{Plot Method for pbayesdecisionprob1bin Objects}
\usage{
\method{plot}{pbayesdecisionprob1bin}(
  x,
  title = NULL,
  xlab = NULL,
  col_go = "#004C97",
  col_nogo = "#F0B323",
  col_gray = "gray60",
  base_size = 28,
  ...
)
}
\arguments{
\item{x}{An object of class \code{pbayesdecisionprob1bin}.}

\item{title}{A character string for the plot title. Defaults to
\code{NULL} (no title displayed).}

\item{xlab}{A character string or expression for the x-axis label.
Defaults to \code{NULL}, which auto-generates a label based on
\code{design}.}

\item{col_go}{A character string specifying the colour for the Go curve.
Default is \code{"#004C97"}.}

\item{col_nogo}{A character string specifying the colour for the NoGo curve.
Default is \code{"#F0B323"}.}

\item{col_gray}{A character string specifying the colour for the Gray curve.
Default is \code{"gray60"}.}

\item{base_size}{A positive numeric scalar specifying the base font size
(in points) passed to \code{theme_bw()}. Default is \code{28}.}

\item{...}{Further arguments passed to or from other methods (ignored).}
}
\value{
Invisibly returns a \code{ggplot} object.
}
\description{
Displays an operating characteristics curve of Go/NoGo/Gray decision
probabilities against the true treatment effect for binary endpoint results
returned by \code{\link{pbayesdecisionprob1bin}}.
}
\details{
For \code{design = 'controlled'} or \code{design = 'external'}, the
x-axis represents the treatment-minus-control difference
\eqn{\theta = \pi_t - \bar{\pi}_c}, where \eqn{\bar{\pi}_c} is the mean
of the supplied \code{pi_c} values. For \code{design = 'uncontrolled'},
the x-axis represents \eqn{\pi_t} directly.

Vertical reference lines are drawn at the decision thresholds:
\itemize{
\item When \code{prob = 'posterior'}: lines at \eqn{\theta_{TV}} and
\eqn{\theta_{MAV}} (converted to the x-axis scale).
\item When \code{prob = 'predictive'}: a single line at \eqn{\theta_{NULL}}.
}
}
